apiVersion: "apps/v1"
kind: "StatefulSet"
metadata:
  name: {{ .Values.monitoringService.appName }}
  namespace: "default"
  labels:
    app: {{ .Values.monitoringService.appName }}
spec:
  replicas: {{ .Values.monitoringService.replicaCount }}
  serviceName: {{ .Values.monitoringService.appName }}
  selector:
    matchLabels:
      app: {{ .Values.monitoringService.appName }}
  template:
    metadata:
      labels:
        app: {{ .Values.monitoringService.appName }}
    spec:
      containers:
      - name: {{ .Values.monitoringService.appName }}
        image: {{ .Values.monitoringService.image }}
        imagePullPolicy: Always
        env:
          - name: "RABBIT_HOST"
            value: "rabbitmq-service.default.svc.cluster.local"
          - name: "COLLECTION_RABBIT_HOST"
            value: "collection-rabbitmq-service.default.svc.cluster.local"
          - name: "RABBIT_PORT"
            value: "5672"
          - name: "RABBIT_USER"
            value: "admin"
          - name: "RABBIT_PASS"
            value: "admin123"
          - name: "REPLICA_ID"
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
        
---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "{{ .Values.monitoringService.appName }}-service"
  namespace: "default"
  labels:
    app: {{ .Values.monitoringService.appName }}
spec:
  ports:
  - protocol: "TCP"
    port: 80
    targetPort: 5001
  selector:
    app: {{ .Values.monitoringService.appName }}
  type: "ClusterIP"
  loadBalancerIP: ""

---
apiVersion: "v1"
kind: "ConfigMap"
metadata:
  name: "{{ .Values.monitoringService.postgresName }}-config"
  namespace: "default"
  labels:
    app: {{ .Values.monitoringService.postgresName }}
data:
  POSTGRES_DB: "monitoring"
  POSTGRES_USER: "simion"
  POSTGRES_PASSWORD: "12345"


---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: {{ .Values.monitoringService.postgresName }}
  namespace: "default"
  labels:
    app: {{ .Values.monitoringService.postgresName }}
spec:
  replicas: {{ .Values.monitoringService.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.monitoringService.postgresName }}
  template:
    metadata:
      labels:
        app: {{ .Values.monitoringService.postgresName }}
    spec:
      containers:
      - name: {{ .Values.monitoringService.postgresName }}
        image: "postgres:9.6.2"
        env:
        - name: "POSTGRES_DB"
          valueFrom:
            configMapKeyRef:
              key: "POSTGRES_DB"
              name: "{{ .Values.monitoringService.postgresName }}-config"
        - name: "POSTGRES_USER"
          valueFrom:
            configMapKeyRef:
              key: "POSTGRES_USER"
              name: "{{ .Values.monitoringService.postgresName }}-config"
        - name: "POSTGRES_PASSWORD"
          valueFrom:
            configMapKeyRef:
              key: "POSTGRES_PASSWORD"
              name: "{{ .Values.monitoringService.postgresName }}-config"
        ports:
          - containerPort: 5432
            name: {{ .Values.monitoringService.postgresName }}
        volumeMounts:
          - name: "{{ .Values.monitoringService.postgresName }}-storage"
            mountPath: /var/lib/postgresql/db-data
      volumes:
        - name: "{{ .Values.monitoringService.postgresName }}-storage"
          persistentVolumeClaim:
            claimName: "{{ .Values.monitoringService.postgresName }}-pv-claim"

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ .Values.monitoringService.postgresName }}-pv-claim"
  labels:
    app: "{{ .Values.monitoringService.postgresName }}"
    service: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.monitoringService.postgresName }}
spec:
  ports:
    - port: 5432
  selector:
    app: {{ .Values.monitoringService.postgresName }}